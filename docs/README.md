# Screenshots

Drop two PNGs here so they render in the main README:

- `dashboard.png` — the Streamlit dashboard showing the metric charts.
- `nl2sql.png` — the "Ask your data" box: a question, the generated SQL, and the result table/chart.

How to capture: run the stack (`docker compose up`), open http://localhost:8501,
log in as `admin` / `admin123`, and screenshot the dashboard and the NL-to-SQL box.
